{
  "id": "navigation",
  "name": "Navigating the Filesystem",
  "title": "Navigating the Filesystem",
  "description": "Chapter 6: the filesystem hierarchy, absolute and relative paths, the shortcuts . .. ~ and -, and listing what is there.",
  "questions": [
    {
      "type": "mc",
      "core": true,
      "category": "Filesystem Hierarchy",
      "question": "What makes the Linux filesystem different from the way Windows presents storage?",
      "options": [
        "Everything hangs off a single root directory, /, and other devices are mounted into that one tree",
        "Each device gets its own drive letter, exactly as on Windows",
        "Linux can only use one storage device at a time",
        "Linux stores directories inside files rather than files inside directories"
      ],
      "correct": 0,
      "explanation": "There is one tree. A second disk does not become D: — it is attached at a directory such as /mnt/field, and its contents appear there."
    },
    {
      "type": "matching",
      "core": true,
      "category": "Filesystem Hierarchy",
      "question": "Match each standard directory to what it holds.",
      "pairs": [
        { "left": "/etc", "right": "System-wide configuration files" },
        { "left": "/var", "right": "Variable data: logs, spools, caches, databases" },
        { "left": "/usr", "right": "Installed software: binaries, libraries and documentation" },
        { "left": "/home", "right": "Ordinary users' home directories" },
        { "left": "/tmp", "right": "Temporary files, cleared periodically" },
        { "left": "/boot", "right": "The kernel and what is needed to start the system" }
      ],
      "explanation": "These names are set by the Filesystem Hierarchy Standard, so they mean the same thing on any distribution."
    },
    {
      "type": "matching",
      "category": "Filesystem Hierarchy",
      "question": "Match each of these directories to what it holds.",
      "pairs": [
        { "left": "/dev", "right": "Device files representing hardware" },
        { "left": "/proc", "right": "A virtual view of kernel and process information" },
        { "left": "/root", "right": "The superuser's own home directory" },
        { "left": "/opt", "right": "Optional add-on software from third parties" },
        { "left": "/run", "right": "Runtime data created since the last boot" },
        { "left": "/srv", "right": "Data served by this system to others" }
      ],
      "explanation": "/proc and /run hold nothing on disk — they are generated by the kernel and rebuilt every boot."
    },
    {
      "type": "mc",
      "core": true,
      "category": "Filesystem Hierarchy",
      "question": "On Red Hat Enterprise Linux 9, what is /bin?",
      "options": [
        "A symbolic link to /usr/bin, kept for compatibility with older paths",
        "A separate directory holding a different set of commands from /usr/bin",
        "The directory where users install their own programs",
        "A directory that only exists while the system is booting"
      ],
      "correct": 0,
      "explanation": "RHEL 7 merged /bin, /sbin, /lib and /lib64 into /usr and left symbolic links behind, so a script written for either path still works."
    },
    {
      "type": "mc",
      "core": true,
      "category": "Paths",
      "question": "What is the difference between an absolute and a relative path?",
      "options": [
        "An absolute path starts at / and means the same thing from anywhere; a relative path starts from the current working directory",
        "An absolute path can only name directories; a relative path can only name files",
        "An absolute path is shorter; a relative path is longer",
        "An absolute path works only for root; a relative path works for everyone"
      ],
      "correct": 0,
      "explanation": "The leading slash is the whole distinction. /etc/hosts always names the same file; etc/hosts names one inside wherever you happen to be."
    },
    {
      "type": "matching",
      "core": true,
      "category": "Paths",
      "question": "Match each path shortcut to what it refers to.",
      "pairs": [
        { "left": ".", "right": "The current directory" },
        { "left": "..", "right": "The parent of the current directory" },
        { "left": "~", "right": "Your own home directory" },
        { "left": "~operator1", "right": "The home directory of the user operator1" },
        { "left": "/", "right": "The root of the whole filesystem" },
        { "left": "-", "right": "The directory you were in before this one (with cd)" }
      ],
      "explanation": "`cd -` is the one people forget, and it is the fastest way to bounce between two directories."
    },
    {
      "type": "shell",
      "core": true,
      "category": "Paths",
      "question": "Print the absolute path of the directory you are currently working in.",
      "places": ["~"],
      "solution": "pwd",
      "explanation": "`pwd` stands for print working directory. The shell also keeps it in the $PWD variable."
    },
    {
      "type": "shell",
      "core": true,
      "category": "Paths",
      "question": "Starting from your home directory, change into /var/log using an absolute path, then print where you ended up.",
      "places": ["/var/log"],
      "cwd": "~",
      "solution": "cd /var/log && pwd",
      "explanation": "An absolute path begins with /, so it does not matter where you started from."
    },
    {
      "type": "shell",
      "core": true,
      "category": "Paths",
      "question": "You are in /var/log. Using a relative path only — no leading slash — move up two levels and print where you are.",
      "places": ["/var/log"],
      "cwd": "/var/log",
      "hint": ".. means the parent directory, and it can be repeated.",
      "solution": "cd ../.. && pwd",
      "explanation": "/var/log → /var → /. Each .. climbs one level, and they can be chained in a single path."
    },
    {
      "type": "shell",
      "category": "Paths",
      "question": "From anywhere on the system, return to your own home directory and print the path, using the shortcut for it rather than typing /home/student.",
      "places": ["~"],
      "cwd": "/etc",
      "solution": "cd ~ && pwd",
      "explanation": "A bare `cd` with no argument does the same thing. Both expand to whatever the $HOME variable holds."
    },
    {
      "type": "tf",
      "category": "Paths",
      "question": "The path ./report.txt and the path report.txt name the same file when typed in the same directory.",
      "correct": true,
      "explanation": "The . prefix is redundant for most commands. It matters when running a program: ./script.sh says 'the one here', because the current directory is not on the PATH."
    },
    {
      "type": "mc",
      "core": true,
      "category": "Paths",
      "question": "Why must you type `./script.sh` rather than just `script.sh` to run an executable file in your current directory?",
      "options": [
        "The current directory is deliberately not on the PATH, so the shell will not find the file unless you say where it is",
        "Scripts always require a path prefix, wherever they are",
        "The shell cannot read files in the current directory",
        "It is a convention with no technical effect"
      ],
      "correct": 0,
      "explanation": "Leaving . off the PATH is a security decision: otherwise dropping a file named `ls` into a shared directory would hijack anyone who ran `ls` there."
    },
    {
      "type": "shell",
      "core": true,
      "category": "Listing",
      "question": "List the contents of the /etc/ssh directory in long format, so the permissions, owner, group, size and date are shown.",
      "places": ["/etc/ssh"],
      "solution": "ls -l /etc/ssh",
      "explanation": "The long format is the one that answers 'who owns this and who may touch it', which is why it turns up in nearly every exercise."
    },
    {
      "type": "shell",
      "core": true,
      "category": "Listing",
      "question": "List everything in your home directory including the hidden entries whose names begin with a dot.",
      "places": ["~"],
      "cwd": "~",
      "solution": "ls -a",
      "explanation": "A leading dot is the only thing that makes a file hidden — there is no hidden attribute. `ls -A` does the same but leaves out . and .."
    },
    {
      "type": "shell",
      "category": "Listing",
      "question": "Show the long listing of the /tmp directory itself — its own permissions and ownership — rather than a listing of what is inside it.",
      "places": ["/tmp"],
      "hint": "One option tells ls to treat a directory as the thing being listed.",
      "solution": "ls -ld /tmp",
      "explanation": "Without -d, ls lists a directory's contents. With it, you see the directory's own line — which is how you spot the sticky bit on /tmp."
    },
    {
      "type": "shell",
      "core": true,
      "category": "Listing",
      "question": "List the files in ~/labs/files in long format, sorted so the most recently modified file appears first.",
      "places": ["~/labs/files"],
      "cwd": "~/labs/files",
      "orderMatters": true,
      "solution": "ls -lt",
      "explanation": "-t sorts by modification time, newest first. Add -r to reverse it and put the oldest first."
    },
    {
      "type": "shell",
      "category": "Listing",
      "question": "List the contents of ~/labs/files in long format with the file sizes shown in a human-readable form such as 1K or 4M, rather than in bytes.",
      "places": ["~/labs/files"],
      "cwd": "~/labs/files",
      "solution": "ls -lh",
      "explanation": "-h only changes how the size column is printed. On its own it does nothing, because the short listing has no size column."
    },
    {
      "type": "shell",
      "core": true,
      "category": "Listing",
      "question": "Count how many entries are in the /etc directory, not counting hidden ones. Print only the number.",
      "places": ["/etc"],
      "hint": "ls prints one name per line when its output is piped.",
      "solution": "ls /etc | wc -l",
      "explanation": "When ls writes to a pipe rather than a terminal it switches to one name per line, which is exactly what wc -l needs."
    },
    {
      "type": "shell",
      "category": "Listing",
      "question": "List ~/labs/survey and everything beneath it, recursing into each subdirectory.",
      "places": ["~/labs/survey"],
      "cwd": "~",
      "solution": "ls -R labs/survey",
      "explanation": "-R walks the whole tree. For anything deep, `find` or `tree` is usually easier to read."
    },
    {
      "type": "mc",
      "category": "Listing",
      "question": "In the `ls -l` output line `drwxr-xr-x. 3 student student 4096 Jun 12 16:10 files`, what does the leading `d` mean?",
      "options": [
        "The entry is a directory",
        "The entry has been deleted",
        "The entry is a device file",
        "The entry is a duplicate of another file"
      ],
      "correct": 0,
      "explanation": "The first character is the file type: - for a regular file, d for a directory, l for a symbolic link, b and c for block and character devices."
    },
    {
      "type": "fill_blank",
      "core": true,
      "category": "Listing",
      "question": "Which single-letter option to `ls` shows the entries whose names start with a dot? Give just the option, including its dash.",
      "answers": ["-a", "a"],
      "explanation": "`ls -a` shows everything; `ls -A` shows everything except the . and .. entries."
    },
    {
      "type": "tf",
      "category": "Paths",
      "question": "Because Linux file names are case sensitive, Report.txt and report.txt can both exist in the same directory.",
      "correct": true,
      "explanation": "They are two unrelated files. It is a common way to lose work when moving files from a case-insensitive system."
    },
    {
      "type": "mc",
      "category": "Paths",
      "question": "Which characters are best avoided in Linux file names, and why?",
      "options": [
        "Spaces and shell metacharacters such as * ? $ and &, because the shell will interpret them before the command ever sees the name",
        "Digits, because a file name cannot begin with one",
        "Uppercase letters, because the filesystem lowercases them",
        "Underscores and hyphens, because they are reserved for system files"
      ],
      "correct": 0,
      "explanation": "The filesystem allows almost anything, but the shell processes the line first. A file called `my file*` has to be quoted every time it is mentioned."
    },
    {
      "type": "shell",
      "category": "Paths",
      "question": "There is a file in ~/labs/files whose name contains a space. Display its contents, quoting the name so the shell passes it through as one argument.",
      "places": ["~/labs/files"],
      "cwd": "~/labs/files",
      "hint": "Quotes, or a backslash before the space.",
      "solution": "cat \"spaced name.txt\"",
      "explanation": "Without quoting, the shell splits on the space and cat is asked for two files that do not exist. `cat spaced\\ name.txt` works just as well."
    },
    {
      "type": "short_answer",
      "core": true,
      "category": "Paths",
      "question": "You are logged in as student, working in /home/student/labs/text, and you want to look at /etc/hosts. Give two different commands that would display it, one using an absolute path and one using a relative path, and say which you would actually use and why.",
      "answerLang": null,
      "modelAnswer": "Absolute:\n    cat /etc/hosts\n\nRelative, from /home/student/labs/text:\n    cat ../../../../etc/hosts\n\nI would use the absolute path. It is shorter here, it says plainly which file is meant, and it keeps working if I change directory or paste the command into a script. Relative paths earn their place when the target is close by — ../text/notes.txt from a sibling directory — not when it is on the other side of the tree.",
      "rubric": [
        "Gives a correct absolute path command (/etc/hosts)",
        "Gives a relative path that actually climbs to / from /home/student/labs/text (four levels of ..)",
        "States a preference and gives a reason, such as clarity or working from anywhere"
      ],
      "explanation": "Counting the .. levels is the part worth practising: text → labs → student → home → /, so four."
    },
    {
      "type": "shell",
      "category": "Listing",
      "question": "Show the inode number alongside each entry in ~/labs/links, in long format.",
      "places": ["~/labs/links"],
      "cwd": "~/labs/links",
      "solution": "ls -li",
      "explanation": "The inode number is the file's real identity. Two names sharing one inode number are hard links to the same file."
    },
    {
      "type": "mc",
      "core": true,
      "category": "Filesystem Hierarchy",
      "question": "Where would you look for the log files that record what services on this system have been doing?",
      "options": [
        "/var/log",
        "/etc/log",
        "/usr/log",
        "/tmp/log"
      ],
      "correct": 0,
      "explanation": "/var holds data that changes as the system runs. Logs, mail spools, databases and caches all live there."
    },
    {
      "type": "shell",
      "category": "Filesystem Hierarchy",
      "question": "List the contents of /var/log in long format so you can see which log files exist and who owns them.",
      "places": ["/var/log"],
      "solution": "ls -l /var/log",
      "explanation": "Most of them are owned by root and readable only by root, which is why reading them needs sudo."
    }
  ]
}
